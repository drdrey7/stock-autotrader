# Public web app. The default fixture configuration needs no secret and runs
# fully on the central demo data adapter. Leave VITE_API_BASE_URL blank to keep
# demo mode; a future authenticated public API will document its own setup.
VITE_API_BASE_URL=
VITE_DEMO_MODE=true
