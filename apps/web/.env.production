# Production builds are API-backed. The preview Worker uses same-origin /api/*
# and proxies those reads to the public production Worker.
VITE_API_BASE_URL=
VITE_DEMO_MODE=false
# Optional external AI Web destination. When unset, the private AI Analysis
# route remains the safe fallback.
VITE_AI_WEB_URL=
