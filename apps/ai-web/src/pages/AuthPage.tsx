import { FormEvent, useState } from "react"; import { Link } from "react-router-dom"; import { Shell } from "../components/layout/Shell"; export function AuthPage(){const [signup,setSignup]=useState(false);const [sent,setSent]=useState(false);const submit=(e:FormEvent)=>{e.preventDefault();setSent(true)};return <Shell><main className="auth-page page"><div className="auth-intro"><div className="section-kicker">{signup?'Create account':'Welcome back'}</div><h1>Make room for<br/><em>better research.</em></h1><p>Sign in to keep your analysis workspace in one place.</p></div><form className="auth-form" onSubmit={submit}><h2>{signup?'Create your workspace':'Sign in'}</h2><label>Email<input type="email" required placeholder="you@example.com"/></label><label>Password<input type="password" required placeholder="••••••••"/></label><button className="primary-button" type="submit">{signup?'Create account':'Sign in'} <span>↗</span></button>{sent&&<p className="form-message">Backend connection not configured yet.</p>}<button type="button" className="switch-auth" onClick={()=>{setSignup(!signup);setSent(false)}}>{signup?'Already have an account? Sign in':'Need an account? Create one'}</button></form><Link className="text-link" to="/">Back to home</Link></main></Shell>}
